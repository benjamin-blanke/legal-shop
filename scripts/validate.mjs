import fs from'node:fs';import path from'node:path';const root='.';const files=fs.readdirSync(root,{recursive:true});let errors=[];for(const file of files.filter(f=>f.endsWith('.json'))){try{JSON.parse(fs.readFileSync(path.join(root,file),'utf8'))}catch{errors.push('Invalid JSON: '+file)}}for(const file of files.filter(f=>f.endsWith('.html'))){const html=fs.readFileSync(path.join(root,file),'utf8');for(const match of html.matchAll(/(?:href|src)="(\/[^"?#]*)/g)){let target=match[1].slice(1);if(!path.extname(target))target=path.join(target,'index.html');if(!fs.existsSync(path.join(root,target)))errors.push(`${file} -> ${match[1]}`)}}if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Validated ${files.length} project entries.`);
