import{loadProducts}from'../core/api.js';import{money}from'../core/format.js';export async function initCommand(){const panel=document.querySelector('[data-command-panel]'),input=document.querySelector('[data-command-input]'),out=document.querySelector('[data-command-results]');if(!panel)return;const products=await loadProducts();const draw=q=>{const items=q?products.filter(p=>(p.name+p.category+p.description).toLowerCase().includes(q.toLowerCase())).slice(0,8):products.slice(0,4);out.innerHTML=items.map(p=>`<a href="/produkt/?id=${p.slug}"><small>${p.category}</small><h3>${p.name}</h3><span>${money(p.price)}</span></a>`).join('')};document.querySelector('[data-command]')?.addEventListener('click',()=>{panel.classList.add('open');input.focus();draw('')});document.querySelector('[data-command-close]')?.addEventListener('click',()=>panel.classList.remove('open'));input.addEventListener('input',e=>draw(e.target.value));document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key==='k'){e.preventDefault();panel.classList.add('open');input.focus();draw('')}if(e.key==='Escape')panel.classList.remove('open')})}
